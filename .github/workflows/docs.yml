name: docs

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:

jobs:
  build:
    uses: OpenAutomatedDriving/dev-environment/.github/workflows/docs.yml@main
    secrets: inherit
