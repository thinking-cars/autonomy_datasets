name: consistency

on:
  push:
  workflow_dispatch:

jobs:
  consistency:
    uses: oads-org/openads-dev-environment/.github/workflows/consistency.yml@submodule # TODO: revert to main
    secrets: inherit
